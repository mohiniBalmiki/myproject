version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend/temp
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/temp/build
    files:
      - '**/*'
  cache:
    paths:
      - frontend/temp/node_modules/**/*
